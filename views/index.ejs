<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %><% if (user) {%>, <%= user.name %>! <%} %></p>
    <ul class="right">
      <li>
      <% if (user) { %>
        <a href="/logout">Log Out</a>
      <% } else { %>
        <a href="/auth/google">Login with Google</a>
      <% } %>
      </li>
    </ul>
    <form action="/teams" id="select-team" method="GET">
      <button>Find your Team</button>
      
    </form>
    
  </body>
</html>
